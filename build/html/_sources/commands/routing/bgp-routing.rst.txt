BGP Routing Protocol
====================

BGP (Border Gate Protocol)

IPv4 usage
^^^^^^^^^^

.. code-block :: none
    
    S1(config) router bgp [as-number]
    S1(config) neighbor [ip address of neighbor] remote-as [remote as-number]
    S1(config) network [IP] mask [subnet mask]


IPv6 usage
^^^^^^^^^^